# TODO

直近で着手予定の実装タスク。設計メモ・実装検討を含む。

機能ロードマップ全体は [features.md](features.md) を参照。
features.md の未実装項目のうち具体化したものをここに記載する。

---

## ハイブリッド検索のデフォルト化

### 背景

現在、全文検索・ベクトル検索・ハイブリッド検索が並列の選択肢として存在する。ハイブリッド検索が最も高精度な結果を返すため、これを通常の検索モードとする。

### 設計方針

- ハイブリッド検索（全文検索＋ベクトル検索の併用）を通常検索とする
- ベクトル検索が不能な場合（embeddingモデル未ダウンロード、インデックス未構築等）は全文検索オンリーにフォールバックする

### 検討事項

- UIでのフォールバック状態の表示方法（ユーザーにベクトル検索が無効であることを通知するか）
- フォールバック判定のタイミング（検索実行時 or フォルダ選択時）
- フォールバックからハイブリッド検索への自動復帰（モデルダウンロード完了時等）

---

## 初回ダウンロードウィザード

### 背景

初回起動時にユーザーが手動でダウンロードすべきモデルが2つある。

1. Embeddingモデル（multilingual-e5-small、約470MB）— ベクトル検索に必要
2. LLMモデル（Qwen2.5系、491MB〜9GB）— チャットモードに必要

現在はサイドバーのボタンで個別にダウンロードする必要があり、初回ユーザーには分かりにくい。

### 設計方針

- 初回起動時（モデル未ダウンロード時）にウィザードダイアログを表示する
- ステップ1: Embeddingモデルのダウンロード（必須）
- ステップ2: LLMモデルの選択・ダウンロード（任意、後からでも可）
- 各ステップでプログレスバーを表示する
- ダウンロード完了後にウィザードを閉じ、通常のUIに遷移する

### 検討事項

- ウィザードをスキップする選択肢を提供するか（embeddingモデルなしでも全文検索は使える）
- 2回目以降の起動でウィザードを再表示しない仕組み（モデル存在チェックで判断すれば十分か）

---

## E2EテストのCI統合

### 背景

E2Eテスト（WebdriverIO + tauri-driver）はローカルでは動作するが、CIでの自動実行は未整備。

### 検討事項

- GitHub Actionsでheadless環境でのWebKitWebDriver実行が可能か
- Linux環境でのwebkit2gtk-driverの利用
- テスト用embeddingモデルのキャッシュ戦略（actions/cache）

---

## 品質ベンチマーク

### 検索応答時間

| 対象 | 目標 | 規模 |
|---|---|---|
| 全文検索 | 100ms以内 | 1000ファイル |
| ベクトル検索 | 500ms以内 | 1000ファイル（数万チャンク） |
| ハイブリッド検索 | 1秒以内 | 1000ファイル |
| LLM最初のトークン表示 | 3秒以内 | — |

### 検索精度

| 指標 | 目標 |
|---|---|
| Recall@10 | 80%以上 |

### 検討事項

- ベンチマーク用データセットの作成方法
- criterion クレートによるRustベンチマーク
- 検索精度評価の手動テスト手順の策定
- RAG回答精度の評価基準の定義
- 日本語モデルの品質評価方法

---

## About画面

- アプリ名、バージョン、コピーライトを表示する
- 設定ダイアログ内のセクションまたは独立ダイアログとして実装する

---

## LICENSE画面（OSS表示含む）

- 本アプリのライセンス（MIT）を表示する
- 使用しているOSSライブラリのライセンス一覧を表示する
- Rust依存クレート + npm依存パッケージのライセンスを収集・表示する
- 設定ダイアログ内のセクションまたはAbout画面からの遷移として実装する

