# TODO

直近で着手予定の実装タスク。設計メモ・実装検討を含む。

機能ロードマップ全体は [features.md](features.md) を参照。
features.md の未実装項目のうち具体化したものをここに記載する。

---

## 初回ダウンロードウィザード

### 背景

Embeddingモデルは起動時自動ダウンロードで解決済み。LLMモデルは設定ダイアログから手動選択・ダウンロードが必要。初回ユーザーにはこの手順がわかりにくい。

### 検討事項

- LLMモデル未ダウンロード時にウィザードを表示するか
- システム情報に基づく推奨モデルの自動提案
- ウィザードをスキップする選択肢（LLMなしでも検索は使える）

---

## E2EテストのCI統合

### 背景

E2Eテスト（WebdriverIO + tauri-driver）はローカルでは動作するが、CIでの自動実行は未整備。

### 検討事項

- GitHub Actionsでheadless環境でのWebKitWebDriver実行が可能か
- Linux環境でのwebkit2gtk-driverの利用
- テスト用embeddingモデルのキャッシュ戦略（actions/cache）

---

## 品質ベンチマーク

### 検索応答時間

| 対象 | 目標 | 規模 |
|---|---|---|
| 全文検索 | 100ms以内 | 1000ファイル |
| ベクトル検索 | 500ms以内 | 1000ファイル（数万チャンク） |
| ハイブリッド検索 | 1秒以内 | 1000ファイル |
| LLM最初のトークン表示 | 3秒以内 | — |

### 検索精度

| 指標 | 目標 |
|---|---|
| Recall@10 | 80%以上 |

### 検討事項

- ベンチマーク用データセットの作成方法
- criterion クレートによるRustベンチマーク
- 検索精度評価の手動テスト手順の策定
- RAG回答精度の評価基準の定義
- 日本語モデルの品質評価方法

---

## About画面

- アプリ名、バージョン、コピーライトを表示する
- 設定ダイアログ内のセクションまたは独立ダイアログとして実装する

---

## LICENSE画面（OSS表示含む）

- 本アプリのライセンス（MIT）を表示する
- 使用しているOSSライブラリのライセンス一覧を表示する
- Rust依存クレート + npm依存パッケージのライセンスを収集・表示する
- 設定ダイアログ内のセクションまたはAbout画面からの遷移として実装する

---

## レイアウト改善

### サイドバー折りたたみ

- サイドバーを折りたたみ/展開できるようにする
- 折りたたみ時はメインパネルが全幅を使える

### 検索結果の左右ペインサイズ変更

- ResultList（左）と Preview（右）の境界をドラッグしてサイズを変更できるようにする
- リサイズハンドルの実装（CSS resize または JavaScript ドラッグ）

---

## 複数フォルダの横断検索

フォルダの登録・切り替え・管理は実装済み（Phase 7）。残りは横断検索:

- 複数のフォルダのインデックスを同時に検索できるようにする
- 現在は1フォルダずつの切り替え操作

---

## キャッシュサイズ設定

- ダウンロードだけでなくインデックスキャッシュサイズも含めた「キャッシュサイズ」設定に変える

---

## バンドルファイル名のカスタマイズ

### 背景

productName が "Folder Search" のため、バンドルファイル名が `Folder.Search_x.y.z_amd64.deb` のようにドット区切りになる。`folder-search_x.y.z_amd64.deb` のような小文字ハイフン区切りが望ましい。

### 現状

Tauri v2.10 では `tauri.conf.json` にバンドルファイル名をカスタマイズするオプションが存在しない。productName から自動生成される仕組みが固定されている。

### 検討事項

- tauri-apps/tauri#13999 でカスタムパッケージ名の要望が出ているが未実装
- Tauri の将来バージョンでの対応を待つ
- 代替案: ワークフロー内でビルド後にリネームする方法（tauri-action のアップロード無効化が必要）

---

## macOS Intel (x86_64) ビルド対応

### 背景

ort-sys 2.0.0-rc.12がx86_64-apple-darwin向けのプリビルドバイナリを提供していないため、macOS Intelのリリースビルドを一時除外している。

### 検討事項

- ort-sysの将来バージョンでのx86_64対応状況を確認
- ONNX Runtimeをソースからビルドする方法
- ortの`load-dynamic` featureでリンクをバイパスしDLLを別途配布する方法

